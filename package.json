{
  "name": "fibo",
  "private": true,
  "version": "1.0.0",
  "description": "Ephemeral story-point planning sessions for software teams",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "preview": "npm run preview -w apps/web",
    "lint": "oxlint && npm run lint -w apps/web",
    "format": "prettier --write .",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w apps/web",
    "test": "npm run test -w packages/shared && npm run test -w apps/web && npm --prefix functions test",
    "emulators": "firebase emulators:start --project demo-fibo",
    "icons": "node scripts/make-icons.mjs"
  },
  "devDependencies": {
    "oxlint": "^1.78.0",
    "prettier": "^3.6.2",
    "typescript": "^6.0.2"
  }
}
